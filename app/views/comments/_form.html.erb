<%= form_with url: '/comments',action: :create, method: :post do |form| %>
    <%# <%form.hidden_field :post_id, value: params[post_id]%> 
  <p>
    <%# <%= current_user.name%> 
  </p>
  <p>
    <%= form.text_area :body, placeholder: "Comment.." %>
  </p>
  <p>
    <%=form.hidden_field :commentable_type, value: type %>
    <%=form.hidden_field :commentable_id, value: id  %>
  </p>
  <p>
    <%= form.submit "Post this comment", class: "btn btn-dark"%>
  </p>
</p>
<% end %>
